apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: requestintent2default
  namespace: default
spec:
  policyTypes:
  - Ingress
  podSelector: {}
  ingress:
  - from:
    - namespaceSelector:
        matchLabels:
          name: fluidos
      podSelector:
        matchLabels:
          app: bank-payment
