apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: test1milan7
  namespace: milan
spec:
  policyTypes:
  - Egress
  podSelector: {}
  egress:
  - ports:
    - port: 80
      protocol: TCP
    to:
    - namespaceSelector:
        matchLabels:
          name: default-rome-a2d9d2
      podSelector:
        matchLabels:
          app: product-catalogue
