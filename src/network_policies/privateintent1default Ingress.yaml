apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: privateintent1default
  namespace: default
spec:
  policyTypes:
  - Ingress
  podSelector:
    matchLabels:
      app: product-catalogue
  ingress:
  - from:
    - namespaceSelector: {}
      podSelector: {}
    ports:
    - port: 80
      protocol: TCP
