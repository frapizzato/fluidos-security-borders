apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: requestintent1fluidos
  namespace: fluidos
spec:
  policyTypes:
  - Egress
  podSelector:
    matchLabels:
      app: order-placement
  egress:
  - to:
    - namespaceSelector:
        matchLabels:
          name: default
      podSelector:
        matchLabels:
          app: product-catalogue
