apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: RequestIntent_4
  namespace: fluidos
spec:
  ingress:
  - from:
    - namespaceSelector:
        matchLabels:
          name: monitoring
      podSelector: {}
  podSelector:
    matchLabels:
      app: order_placement
  policyTypes:
  - Ingress
