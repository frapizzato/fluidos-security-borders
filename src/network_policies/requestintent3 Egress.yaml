apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: requestintent3
  namespace: fluidos
spec:
  policyTypes:
  - Egress
  podSelector:
    matchLabels:
      app: bank_payment
  egress:
  - to:
    - ipBlock:
        cidr: 0.0.0.0/0
