apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: requestintent4
  namespace: monitoring
spec:
  policyTypes:
  - Egress
  podSelector: {}
  egress:
  - to:
    - namespaceSelector:
        matchLabels:
          name: fluidos
      podSelector:
        matchLabels:
          app: order-placement
