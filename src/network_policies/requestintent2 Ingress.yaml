apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: requestintent2
  namespace: default
spec:
  policyTypes:
  - Ingress
  podSelector: {}
  ingress:
  - from:
    - namespaceSelector:
        matchLabels:
          name: fluidos
      podSelector:
        matchLabels:
          app: bank_payment
